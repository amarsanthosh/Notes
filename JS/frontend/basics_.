

npm ci -> clean install

//backend

//index.js
const dotenv = require('dotenv');
dotenv.config();
const PORT = process.env.PORT || 3000;

copy and paste all the dependecies from avinash
sweetalert - > dependency very use ful for popping up messages

